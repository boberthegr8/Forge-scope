(() => {
  const S = { plans: [], existingBom: null, type: 'auto', running: false, step: 0, error: '', scopeResult: null, takeoffResult: null, reportResult: null };
  const e=v=>(typeof esc==='function'?esc(v??''):String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])));
  const i=(n,c='w-4 h-4')=>typeof icon==='function'?icon(n,c):'';
  function schemaSummary(type){const out={};if(typeof SCHEMAS==='undefined')return out;const keys=type&&type!=='auto'&&SCHEMAS[type]?[type]:Object.keys(SCHEMAS);keys.forEach(k=>out[k]=(SCHEMAS[k]||[]).map(s=>({section:s.title,fields:(s.fields||[]).map(f=>({key:f.key,label:f.label,type:f.type||'text',options:f.options||[]}))})));return out;}
  function typeOptions(){const a=[`<option value="auto" ${S.type==='auto'?'selected':''}>Auto-detect from plans</option>`];if(typeof TYPES!=='undefined')Object.entries(TYPES).forEach(([k,v])=>a.push(`<option value="${e(k)}" ${S.type===k?'selected':''}>${e(v.label)}</option>`));return a.join('');}
  function setField(scope,key,value,source='Automatic Scope AI',status='Confirmed'){if(!scope?.fields?.[key]||value==null||String(value).trim()==='')return false;scope.fields[key]={...scope.fields[key],value:String(value),source,status,reviewed:false};return true;}
  function normalize(r){if(!r)return r;const rows=r.recommendedBOM||r.correctedBOM||[];return {verdict:r.verdict||{status:'REVIEW COMPLETE',summary:r.summary||'AI plan takeoff and scope comparison complete.'},scopeCorrections:r.scopeCorrections||[],scopeUpdates:r.scopeUpdates||{},remove:r.remove||[],keep:r.keep||[],add:r.add||[],changeSpec:r.changeSpec||[],verify:r.verify||[],correctedBOM:rows.map(x=>({category:x.category||'Other',material:x.material||x.item||'',quantity:x.newQty??x.quantity??'TBC',unit:x.unit||'',action:x.change||x.action||'KEEP',source:x.source||'',confidence:x.confidence||'',location:x.location||'Location TBC',oldQty:x.oldQty??'',newQty:x.newQty??x.quantity??''})),warnings:r.warnings||[]};}
  function save(){if(!current)return;current.autoScopeAI={...(current.autoScopeAI||{}),scope:S.scopeResult,takeoff:S.takeoffResult,report:S.reportResult,updatedAt:new Date().toISOString()};if(S.reportResult)current.bomComparison={...(current.bomComparison||{}),result:normalize(S.reportResult),analyzedAt:new Date().toISOString(),bomName:S.existingBom?.name||'AI Plan Takeoff',planNames:S.plans.map(f=>f.name),model:typeof geminiCompareModel!=='undefined'?geminiCompareModel:'Gemini'};if(typeof upsertCurrent==='function')upsertCurrent();}
  function applyScope(){const r=S.scopeResult;if(!r)return;const t=r.templateType&&typeof SCHEMAS!=='undefined'&&SCHEMAS[r.templateType]?r.templateType:(S.type!=='auto'?S.type:null);if(!t||typeof newProject!=='function')throw new Error('AI could not identify a supported Forge Scope template.');const scope=newProject(t,'ai');Object.entries(r.fields||{}).forEach(([k,v])=>{if(v&&typeof v==='object'&&!Array.isArray(v))setField(scope,k,v.value,v.source||'Plans',v.status||((v.confidence||'').toLowerCase()==='low'?'Verify':'Confirmed'));else setField(scope,k,v,'Plans','Confirmed');});if(scope.fields?.projectName&&!scope.fields.projectName.value)setField(scope,'projectName',r.projectName||S.plans[0]?.name?.replace(/\.[^.]+$/,''),'Plans','Confirmed');if(scope.fields?.rfi&&Array.isArray(r.rfis)&&r.rfis.length)setField(scope,'rfi',r.rfis.map(x=>typeof x==='string'?x:(x.question||x.item||JSON.stringify(x))).join('\n'),'Automatic Scope AI','RFI');current=scope;save();}
  function fillBlanks(){if(!current||!S.reportResult)return 0;let n=0;Object.entries(S.reportResult.scopeUpdates||{}).forEach(([k,v])=>{const val=v&&typeof v==='object'?v.value:v;if(current.fields?.[k]&&!String(current.fields[k].value||'').trim()&&String(val??'').trim()){setField(current,k,val,(v&&v.source)||'AI cross-check',(v&&v.status)||'Confirmed');n++;}});save();return n;}
  async function ask(parts,text){if(typeof geminiSessionKey==='undefined'||!String(geminiSessionKey||'').trim())throw new Error('Add your Gemini API key in Forge Scope AI settings first.');if(typeof fileToGeminiPart!=='function')throw new Error('Forge file AI helper is not available. Refresh and try again.');const st=document.getElementById('autoScopeStatus');if(st)st.textContent=text||'AI is working...';const model=typeof geminiCompareModel!=='undefined'?geminiCompareModel:'gemini-2.5-pro';const response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`,{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':String(geminiSessionKey).trim()},body:JSON.stringify({contents:[{role:'user',parts}],generationConfig:{temperature:.1,responseMimeType:'application/json'}})});const p=await response.json().catch(()=>({}));if(!response.ok)throw new Error(p?.error?.message||`Gemini request failed (${response.status}).`);const raw=(p?.candidates?.[0]?.content?.parts||[]).map(x=>x.text||'').join('');return typeof parseAIJson==='function'?parseAIJson(raw):JSON.parse(raw);}
  async function planParts(prompt){const parts=[{text:prompt}];for(let x=0;x<S.plans.length;x++){const f=S.plans[x],st=document.getElementById('autoScopeStatus');if(st)st.textContent=`Preparing plan ${x+1} of ${S.plans.length}: ${f.name}`;parts.push({text:`PLAN / DRAWING FILE ${x+1}: ${f.name}`});parts.push(await fileToGeminiPart(f));}return parts;}
  async function scopeAI(){const prompt=`You are Forge Scope AI, an expert Canadian construction/LBM estimator. Analyze the attached construction drawings and automatically identify the correct Forge Scope template and fill the worksheet from plan evidence.\n\nAVAILABLE FORGE SCHEMAS:\n${JSON.stringify(schemaSummary(S.type))}\n\nRULES:\n- If template selection is auto, choose the best templateType from the supplied schema keys.\n- Read plans, elevations, sections, schedules, notes, details and structural sheets together.\n- Fill only facts supported by drawings. Never invent dimensions or assemblies.\n- Every field includes value, source, confidence High/Medium/Low and status Confirmed/Verify/RFI.\n- Put unclear or conflicting items in rfis.\n- Identify quote scope, exclusions, project geometry, framing, openings, exterior materials and structural conditions where supported.\nRETURN JSON ONLY:\n{"templateType":"schema key","projectName":"","fields":{"fieldKey":{"value":"","source":"sheet/detail","confidence":"High","status":"Confirmed"}},"rfis":[{"question":"","source":""}],"summary":""}`;S.scopeResult=await ask(await planParts(prompt),'AI is identifying the Scope of Work from the plans...');applyScope();save();}
  async function takeoffAI(){const prompt=`You are Forge Takeoff AI, an expert construction material estimator. Perform a material takeoff directly from the attached plans using the extracted Forge Scope below as the scope guardrail.\n\nFORGE SCOPE:\n${JSON.stringify(S.scopeResult||{})}\n\nRULES:\n- Include only materials in supported scope unless clearly VERIFY.\n- Calculate from dimensions, spacing, counts, elevations, sections, schedules and details.\n- NEVER return material without a location. Location is mandatory.\n- Split identical materials into separate rows when used in different building locations/assemblies.\n- Use estimator-friendly locations such as Basement - Floor Load, Basement - Wall Load, 1st Floor - Floor Load, 1st Floor - Wall Load, 2nd Floor - Floor Load, 2nd Floor - Wall Load, Roof, Front/Rear Elevation, W1/W2, Gable Ends, Headers, Garage, Deck; prefer actual drawing tags.\n- Include calculation and source for every line.\n- If quantity cannot be proven use TBC and explain; do not fabricate.\nRETURN JSON ONLY:\n{"takeoff":[{"location":"","category":"","material":"","quantity":"","unit":"pcs/LF/sheets/sqft/etc","calculation":"","source":"sheet/detail","confidence":"High","status":"TAKEOFF/VERIFY"}],"assumptions":[],"rfis":[],"summary":""}`;S.takeoffResult=await ask(await planParts(prompt),'AI is performing the plan material takeoff...');save();}
  async function reportAI(){const old=S.existingBom?'An existing BOM file is attached. Treat it as OLD BOM and compare it to the plan takeoff.':'No existing BOM is supplied. Treat OLD BOM quantity as 0 for AI-discovered lines unless supported otherwise.';const prompt=`You are Forge Audit AI. Cross-check the extracted Forge Scope against the AI plan takeoff and produce an estimator change report plus complete recommended BOM.\n\nSCOPE:\n${JSON.stringify(S.scopeResult||{})}\n\nAI PLAN TAKEOFF:\n${JSON.stringify(S.takeoffResult||{})}\n\n${old}\n\nRULES:\n- Scope and plans control what belongs in quote.\n- Report missing materials, quantity differences, spec changes, removals, conflicts and unknowns.\n- Fill BLANK scope fields when reliable evidence exists. Never overwrite nonblank estimator values; disagreements go in scopeCorrections.\n- recommendedBOM is the NEW complete working BOM, not just deltas.\n- EVERY recommendedBOM row MUST include location.\n- Keep OLD and NEW quantities visible. Missing => oldQty 0. Removed => newQty 0. Unchanged => same old/new if old BOM exists.\n- Every changed line includes reason and source.\n- Never invent location splits or quantities; use TBC/VERIFY.\nRETURN JSON ONLY:\n{"verdict":{"status":"READY / REVIEW / DO NOT QUOTE","summary":""},"scopeUpdates":{"fieldKey":{"value":"","source":"","status":"Confirmed"}},"scopeCorrections":[{"field":"","current":"","corrected":"","reason":"","source":"","confidence":"High"}],"remove":[],"keep":[],"add":[],"changeSpec":[],"verify":[],"changes":[{"location":"","material":"","oldQty":"","newQty":"","unit":"","change":"ADD/REMOVE/INCREASE/DECREASE/CHANGE/KEEP/VERIFY","reason":"","source":"","confidence":"High"}],"recommendedBOM":[{"location":"","category":"","material":"","oldQty":"","newQty":"","unit":"","change":"KEEP/ADD/REMOVE/CHANGE/VERIFY","reason":"","source":"","confidence":"High"}],"warnings":[],"summary":""}`;const parts=[{text:prompt}];if(S.existingBom){parts.push({text:`OLD / EXISTING BOM FILE: ${S.existingBom.name}`});parts.push(await fileToGeminiPart(S.existingBom));}S.reportResult=await ask(parts,'AI is comparing Scope, plan takeoff and existing BOM...');fillBlanks();save();}
  async function run(){if(S.running)return;S.error='';if(!S.plans.length){S.error='Attach at least one plan/drawing file.';return render();}S.running=true;S.step=1;render();try{await scopeAI();S.step=2;render();await takeoffAI();S.step=3;render();await reportAI();S.step=4;render();if(typeof notifySaved==='function')notifySaved('Automatic AI Scope + Takeoff + BOM audit complete');}catch(err){S.error=err?.message||String(err);}finally{S.running=false;render();}}
  const q=(x,k)=>x?.[k]===undefined||x?.[k]===null||x?.[k]===''?'—':String(x[k]);
  function step(n,t,d){const done=S.step>n||S.step===4,active=S.running&&S.step===n;return `<div class="auto-step ${done?'done':''} ${active?'active':''}"><div class="auto-step-num">${done?'✓':n}</div><div><b>${t}</b><span>${d}</span></div></div>`;}
  function tables(){const r=S.reportResult||current?.autoScopeAI?.report;if(!r)return '';const ch=r.changes||[],bom=r.recommendedBOM||r.correctedBOM||[];return `<section class="auto-card auto-result"><div class="auto-head"><div><div class="auto-kicker">AI AUDIT REPORT</div><h3>Scope vs Takeoff ${S.existingBom?'vs Existing BOM':''}</h3></div><span class="auto-pill">${e(r.verdict?.status||'Complete')}</span></div>${r.verdict?.summary?`<p>${e(r.verdict.summary)}</p>`:''}<div class="auto-metrics"><div><span>Changes</span><b>${ch.length}</b></div><div><span>Recommended BOM</span><b>${bom.length}</b></div><div><span>Scope blanks filled</span><b>${Object.keys(r.scopeUpdates||{}).length}</b></div><div><span>Warnings</span><b>${(r.warnings||[]).length}</b></div></div>${ch.length?`<div class="auto-table"><h4>Change Report</h4><table><thead><tr><th>Location</th><th>Material</th><th>Old</th><th>New</th><th>Change</th><th>Reason / Source</th></tr></thead><tbody>${ch.map(x=>`<tr><td data-label="Location">${e(x.location||'Location TBC')}</td><td data-label="Material"><b>${e(x.material||'')}</b></td><td data-label="Old">${e(q(x,'oldQty'))} ${e(x.unit||'')}</td><td data-label="New">${e(q(x,'newQty'))} ${e(x.unit||'')}</td><td data-label="Change"><b>${e(x.change||'')}</b></td><td data-label="Reason / Source">${e(x.reason||'')}<small>${e(x.source||'')}</small></td></tr>`).join('')}</tbody></table></div>`:''}${bom.length?`<div class="auto-table"><div class="auto-table-title"><h4>New Recommended BOM</h4><span>Location required on every line</span></div><table><thead><tr><th>Location</th><th>Category</th><th>Material</th><th>Old BOM</th><th>New BOM</th><th>Action</th><th>Source</th></tr></thead><tbody>${bom.map(x=>`<tr><td data-label="Location"><b>${e(x.location||'Location TBC')}</b></td><td data-label="Category">${e(x.category||'Other')}</td><td data-label="Material">${e(x.material||x.item||'')}</td><td data-label="Old BOM">${e(q(x,'oldQty'))} ${e(x.unit||'')}</td><td data-label="New BOM"><b>${e(q(x,'newQty')==='—'?q(x,'quantity'):q(x,'newQty'))} ${e(x.unit||'')}</b></td><td data-label="Action">${e(x.change||x.action||'KEEP')}</td><td data-label="Source">${e(x.source||'—')}</td></tr>`).join('')}</tbody></table></div>`:''}<div class="auto-actions no-print"><button onclick="autoScopeOpenWorksheet()">${i('clipboard-list')} Open AI Scope</button><button onclick="autoScopeOpenComparison()">${i('git-compare-arrows')} Open BOM Comparison</button><button onclick="window.print()">${i('printer')} Print / PDF</button></div></section>`;}
  function html(){return `<div class="auto-shell fade-in"><section class="auto-hero"><div class="auto-kicker">FORGE SCOPE • AUTOMATIC AI</div><h1>Plans → Scope → Takeoff → Audit → Recommended BOM</h1><p>Attach drawings once. Forge AI identifies the scope, performs the material takeoff, cross-checks the work, fills supported blanks, and produces a location-tagged recommended BOM.</p></section><div class="auto-layout"><section class="auto-card"><div class="auto-head"><div><h3>1. Attach Plans</h3><p>PDFs and drawing images can be combined.</p></div>${i('sparkles','w-5 h-5')}</div><label class="auto-drop"><input type="file" multiple accept="application/pdf,image/*" onchange="autoScopePlans(this.files)"><span>${i('upload-cloud','w-6 h-6')}<b>${S.plans.length?`${S.plans.length} plan file${S.plans.length===1?'':'s'} ready`:'Choose plans / drawings'}</b><small>${S.plans.length?S.plans.map(f=>f.name).join(' • '):'Select one or more plan files'}</small></span></label><div class="auto-grid"><label>Scope Template<select onchange="autoScopeType(this.value)">${typeOptions()}</select></label><label>Existing BOM <small>(optional)</small><input type="file" accept="application/pdf,.csv,.xlsx,.xls,.txt,image/*" onchange="autoScopeExistingBom(this.files[0])"><em>${e(S.existingBom?.name||'No old BOM attached')}</em></label></div><button class="auto-run" onclick="autoScopeRun()" ${S.running?'disabled':''}>${S.running?i('loader-circle','w-5 h-5 animate-spin'):i('wand-sparkles','w-5 h-5')} ${S.running?'Forge AI is working...':'Run Automatic Scope AI'}</button><div id="autoScopeStatus" class="auto-status">${e(S.running?'Analyzing project…':S.step===4?'AI workflow complete.':'Ready for plans.')}</div>${S.error?`<div class="auto-error">${e(S.error)}</div>`:''}</section><section class="auto-card auto-progress"><h3>Automatic Workflow</h3>${step(1,'Identify Scope','AI reads the drawings and fills the matching Forge Scope worksheet.')}${step(2,'Material Takeoff','AI builds a location-tagged material list from the plans.')}${step(3,'Cross-check & Fill Blanks','Scope and takeoff are audited; supported blanks are completed.')}${step(4,'Recommended BOM','Old vs new changes and final location-tagged BOM are produced.')}</section></div>${S.scopeResult?`<section class="auto-card auto-mini"><div><div class="auto-kicker">AI SCOPE FOUND</div><h3>${e((typeof TYPES!=='undefined'&&TYPES[S.scopeResult.templateType]?.label)||S.scopeResult.templateType||'Scope')}</h3><p>${e(S.scopeResult.summary||'Scope worksheet created from plan evidence.')}</p></div><button onclick="autoScopeOpenWorksheet()">Open Worksheet</button></section>`:''}${S.takeoffResult?`<section class="auto-card auto-mini"><div><div class="auto-kicker">PLAN TAKEOFF</div><h3>${e((S.takeoffResult.takeoff||[]).length)} material lines identified</h3><p>${e(S.takeoffResult.summary||'Each supported material line is tagged with where it goes.')}</p></div></section>`:''}${tables()}</div>`;}
  function styles(){if(document.getElementById('forge-auto-scope-style'))return;const s=document.createElement('style');s.id='forge-auto-scope-style';s.textContent=`.auto-shell{max-width:1450px;margin:auto;padding-bottom:48px}.auto-hero{background:linear-gradient(135deg,#111318,#1b1e24);border:1px solid #292d35;border-radius:20px;padding:28px;color:#fff;margin-bottom:18px}.auto-hero h1{font-size:clamp(24px,4vw,42px);line-height:1.08;font-weight:850;max-width:950px;margin:6px 0 10px}.auto-hero p{color:#aeb4bf;max-width:930px;line-height:1.65}.auto-kicker{font-size:10px;font-weight:900;letter-spacing:.2em;color:#ff7617}.auto-layout{display:grid;grid-template-columns:minmax(0,1.65fr) minmax(280px,.75fr);gap:18px}.auto-card{background:#fff;border:1px solid #dde2e8;border-radius:18px;padding:20px;box-shadow:0 3px 14px rgba(15,23,42,.04)}.auto-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.auto-card h3{font-size:18px;font-weight:800}.auto-card h4{font-size:13px;text-transform:uppercase;letter-spacing:.09em;font-weight:850;color:#64748b;margin:18px 0 8px}.auto-card p{color:#64748b;font-size:13px;margin-top:4px}.auto-drop{display:block;margin-top:16px;border:2px dashed #cbd5e1;border-radius:15px;background:#f8fafc;cursor:pointer}.auto-drop input{display:none}.auto-drop span{min-height:125px;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;padding:18px;color:#475569}.auto-drop b{margin-top:7px;color:#0f172a}.auto-drop small{margin-top:5px;max-width:680px;color:#94a3b8}.auto-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:14px}.auto-grid label{font-size:12px;font-weight:750;color:#475569}.auto-grid select,.auto-grid input{width:100%;margin-top:6px;border:1px solid #d7dee7;border-radius:10px;padding:11px;background:#fff;font-size:14px}.auto-grid em{display:block;margin-top:5px;font-size:11px;font-style:normal;font-weight:500;color:#94a3b8;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.auto-run{width:100%;display:flex;align-items:center;justify-content:center;gap:9px;margin-top:16px;border:0;border-radius:12px;background:#ff7617;color:#fff;padding:14px 18px;font-weight:850;font-size:15px}.auto-run:disabled{opacity:.6}.auto-status{font-size:12px;color:#64748b;text-align:center;margin-top:9px}.auto-error{margin-top:10px;border:1px solid #fecaca;background:#fff1f2;color:#be123c;padding:10px;border-radius:10px;font-size:12px}.auto-step{display:flex;gap:11px;padding:13px 0;border-bottom:1px solid #eef2f7}.auto-step:last-child{border-bottom:0}.auto-step-num{width:30px;height:30px;flex:0 0 30px;border-radius:50%;display:flex;align-items:center;justify-content:center;background:#eef2f7;color:#64748b;font-size:12px;font-weight:850}.auto-step b{display:block;font-size:13px}.auto-step span{display:block;color:#94a3b8;font-size:11px;line-height:1.45;margin-top:2px}.auto-step.done .auto-step-num{background:#dcfce7;color:#15803d}.auto-step.active .auto-step-num{background:#ffedd5;color:#c2410c}.auto-mini{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:18px}.auto-mini button,.auto-actions button{display:inline-flex;align-items:center;gap:7px;border:1px solid #d7dee7;background:#fff;border-radius:10px;padding:9px 12px;font-size:12px;font-weight:750}.auto-result{margin-top:18px}.auto-pill{font-size:11px;font-weight:850;background:#f1f5f9;border-radius:999px;padding:7px 10px}.auto-metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:14px}.auto-metrics>div{background:#f8fafc;border:1px solid #eef2f7;border-radius:10px;padding:10px}.auto-metrics span{display:block;font-size:10px;text-transform:uppercase;color:#94a3b8}.auto-metrics b{display:block;font-size:18px;margin-top:2px}.auto-table{overflow-x:auto}.auto-table-title{display:flex;align-items:end;justify-content:space-between}.auto-table-title span{font-size:10px;color:#94a3b8}.auto-table table{width:100%;min-width:900px;border:1px solid #e2e8f0;border-collapse:separate;border-spacing:0;border-radius:11px;overflow:hidden}.auto-table th{font-size:10px;text-transform:uppercase;letter-spacing:.06em;color:#94a3b8;background:#f8fafc;text-align:left;padding:9px}.auto-table td{font-size:12px;color:#475569;padding:10px;border-top:1px solid #eef2f7;vertical-align:top}.auto-table td small{display:block;color:#94a3b8;margin-top:3px}.auto-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:16px}@media(max-width:850px){#content{padding:12px!important}.auto-shell{width:100%}.auto-hero{padding:20px 16px;border-radius:15px}.auto-layout{grid-template-columns:1fr}.auto-card{border-radius:14px;padding:15px}.auto-grid{grid-template-columns:1fr}.auto-metrics{grid-template-columns:1fr 1fr}.auto-mini{align-items:flex-start;flex-direction:column}.auto-mini button{width:100%;justify-content:center}.auto-table{overflow:visible}.auto-table table{min-width:0;border:0}.auto-table thead{display:none}.auto-table tbody,.auto-table tr,.auto-table td{display:block;width:100%}.auto-table tr{border:1px solid #e2e8f0;border-radius:12px;margin:10px 0;overflow:hidden;background:#fff}.auto-table td{display:grid;grid-template-columns:105px 1fr;gap:8px;border-top:1px solid #f1f5f9;padding:9px 10px}.auto-table td:first-child{border-top:0}.auto-table td:before{content:attr(data-label);font-size:9px;text-transform:uppercase;letter-spacing:.07em;font-weight:850;color:#94a3b8}.auto-actions{display:grid;grid-template-columns:1fr}.auto-actions button{justify-content:center}.auto-drop span{min-height:105px}}`;document.head.appendChild(s);}
  function sidebar(){const nav=document.querySelector('aside nav');if(!nav||document.getElementById('forge-auto-scope-link'))return;const first=nav.querySelector('button'),h=`<button id="forge-auto-scope-link" onclick="autoScopeOpen()" class="w-full flex items-center justify-between px-4 py-3 rounded-lg text-sm transition text-orange-300 hover:text-white hover:bg-orange-500/10"><span class="flex items-center gap-3">${i('sparkles','w-5 h-5')}<span class="font-semibold">Automatic Scope AI</span></span><span class="text-[9px] font-black tracking-wider">AI</span></button>`;if(first)first.insertAdjacentHTML('beforebegin',h);else nav.insertAdjacentHTML('beforeend',h);}
  window.autoScopeOpen=()=>{view='autoScopeAI';render();};window.autoScopePlans=files=>{S.plans=Array.from(files||[]);S.step=0;S.scopeResult=S.takeoffResult=S.reportResult=null;render();};window.autoScopeExistingBom=f=>{S.existingBom=f||null;render();};window.autoScopeType=v=>S.type=v||'auto';window.autoScopeRun=run;window.autoScopeOpenWorksheet=()=>{if(current){view='editor';render();}};window.autoScopeOpenComparison=()=>{if(!current)return;view='bom';if(typeof bomCompareState!=='undefined'&&current.bomComparison?.result)bomCompareState.result=current.bomComparison.result;render();};
  window.installForgeAutoScopeAI=function(){styles();sidebar();if(window.__forgeAutoScopeInstalled)return;window.__forgeAutoScopeInstalled=true;if(typeof pageTitle==='function'){const old=pageTitle;pageTitle=function(){return view==='autoScopeAI'?'Automatic Scope AI':old();};}if(typeof render==='function'){const old=render;render=function(){if(view==='autoScopeAI'){const app=document.getElementById('app');if(!app)return;app.innerHTML=layoutShell(html());sidebar();window.forgeSuiteInject?.();window.lucide?.createIcons();return;}old();sidebar();};}sidebar();};
})();